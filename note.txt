2019.03.06:
	place函数：
		轮廓点的接续需要完善；
		判断是否正确放置可以通过放置前轮廓面积之差与放置后的接合轮廓面积进行比较。
		可以让place函数返回满足放置条件的所有结果。
	强化学习构想：
		用边和角作为特征，对place函数的两个参数进行学习(目标图案和单元块的对应角点ID)

2019.3.10:
	计算面积怎样才能更快更准？drawContour会有藕断丝连的现象，而且也比较慢。源图案面积小的话，丝占的面积就比较大，影响distRatio
	怎样根据放置后的特征进行剪枝？
	place函数应当更快地排除失败的放置方式
	debug:没有resize,导致画布只有图像上半截,见笑了
	计算面积比例的方式还有点问题
	绝对应当消除计算面积时藕断丝连的现象，影响很大

2019.3.11:
	可以减去单元块周长来除丝
	发现bug: place函数中更改了轮廓点的旋转方向，可是计算角度时始终按照逆时针旋转计算;getAngle在接合轮廓的角度计算上也有问题
	解决了上面三个问题中的前两个
	尝试拼七巧板，十分钟内未完成，失败。根据搜索过程来看，主要还是搜索空间过于巨大。如果能根据放置后的特征进行快速剪枝，应该会有很大帮助。